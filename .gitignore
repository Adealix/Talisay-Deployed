# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Ngrok executable (development tool, not needed in repo)
ngrok.exe
ngrok

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local
.env
server/.env

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
env/
ENV/
ml/venv/
ml/.venv/
*.egg-info/
.installed.cfg
*.egg
.pytest_cache/

# ═══════════════════════════════════════════════════════════════
# ML / Training Data Exclusions
# ═══════════════════════════════════════════════════════════════
# Exclude LARGE image datasets but KEEP trained model files

# Large image dataset folders (training/validation/test sets)
ml/data/cnn_dataset/
ml/data/yolo_dataset/
ml/data/kaggle_talisay/
ml/data/training/
ml/data/validation/
ml/data/test/
ml/data/confidence_training/
ml/data/enhanced_training/
ml/test_images/
ml/baseline_green.jpg

# YOLO training runs (large artifacts)
ml/runs/

# Large pretrained YOLO base weights (can download when needed)
# Keep our trained yolo_talisay.pt - it's essential!
ml/yolov8n.pt
ml/yolo26n.pt

# Keep trained models (.keras, .joblib are essential for API)
# Exclude only truly unnecessary formats
ml/models/*.h5
ml/models/*.tflite
ml/models/*.onnx
ml/models/logs/
ml/models/saved_models/
ml/models/confusion_matrix.png

# All .keras models are now included for redundancy and failsafes
# This ensures the ML image processing (core feature) has backups

# Training/Testing/Debug Scripts (not needed for deployment)
ml/train*.py
ml/retrain*.py
ml/test_*.py
ml/debug_*.py
ml/diag_*.py
ml/profile_*.py
ml/check_*.py
ml/analyze_*.py
ml/verify_*.py
ml/time_*.py
ml/auto_annotate_train.py
ml/finetune_with_annotated.py
ml/generate_training_data.py
ml/integrate_and_train.py
ml/simple_finetune.py
ml/*_results.txt
ml/*_results.json

# Keep ONLY these essential ML files for deployment:
# - api.py (Flask API server)
# - predict.py (prediction logic)
# - config.py (configuration)
# - requirements.txt (dependencies)
# - models/*.py (model classes)
# - models/*.keras (trained models)
# - models/*.joblib (trained models)
# - models/*.json (metadata)

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Database
*.sqlite
*.db

# Logs
logs/
*.log

# Build artifacts and temporary files
*.tmp
*.temp
*.cache
.cache/

# Expo build outputs
.expo-shared/
*.apk
*.aab
*.ipa

# Documentation images (if large screenshots exist)
*.gif
screenshots/
docs/images/

# Backup files
*.backup
*.bak
*_backup.*
*_old.*

# Large JSON data files (keep small config JSONs)
ml/data/*.json
ml/*_dataset.csv

# Windows/Mac system files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

# Git merge artifacts
*.orig
